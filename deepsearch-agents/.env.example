# DeepSearch Agents 环境变量示例
# 用法：复制本文件为 .env，再按本机实际情况填写；.env 已被 .gitignore 忽略，请勿提交真实密钥。
#   cp .env.example .env

# ============================================================
# LLM 配置（OpenAI 兼容端点；默认使用 DeepSeek）
# ============================================================
# DeepSeek 的 OpenAI 兼容 base url（注意带 /v1）
OPENAI_BASE_URL=https://api.deepseek.com/v1
# 在 https://platform.deepseek.com/ 创建的 API Key
OPENAI_API_KEY=sk-在此填写你的密钥
# 主智能体（规划 / 路由 / 最终汇总）模型；子智能体（工具调用 / 结果整理）模型。
# 不配置时代码默认回退到 deepseek-flash。
LLM_MAIN_MODEL=deepseek-flash
LLM_FAST_MODEL=deepseek-flash
# 如改用其它 OpenAI 兼容服务（如阿里 DashScope / 本地 vLLM），替换上面三项即可，例如：
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_MAIN_MODEL=qwen-max
# LLM_FAST_MODEL=qwen-turbo

# ============================================================
# 学术检索（arXiv / OpenAlex / Crossref）
# ============================================================
# OpenAlex 免费 API Key（可选）：不填也能用，但免费配额按"出口 IP"共享，
# 代理 / 校园网等共享 IP 下容易触发 429；配 key 后走专属配额（免费、不绑卡）。
# 申请：https://openalex.org/ （在账号设置里生成 API Key）
OPENALEX_API_KEY=
# arXiv、Crossref 无需密钥；代码已内置 mailto 礼貌池标识。

# ============================================================
# SearXNG 元搜索（本机 WSL2 + Docker，默认 http://localhost:8888）
# ============================================================
# SearXNG 对外地址，默认即本机 8888 端口，一般无需修改
SEARXNG_URL=http://localhost:8888
# WSL 发行版名称（wsl -l -q 查看）；Windows 侧通过 wsl_query.sh 桥接查询
WSL_DISTRO=Ubuntu-20.04
# 桥接脚本路径：默认自动定位到项目根 searxng/wsl_query.sh（一般无需配置）；
# 仅在脚本不在默认位置时才需要显式指定，例如：
# SEARXNG_WSL_SCRIPT=D:/path/to/your/repo/searxng/wsl_query.sh
# 传输通道：auto（默认，自动在 HTTP 直连与 WSL 桥接间选择）/ http / wsl
SEARXNG_TRANSPORT=auto

# ------------------------------------------------------------
# 说明：以下为历史遗留的可选模块，当前主链路未使用，默认无需配置
# - RAGFlow 知识库（app/ragflow/、knowledge_demo.py）：RAGFLOW_API_URL / RAGFLOW_API_KEY
# - MySQL / Tavily：当前检索与存储均不依赖，requirements 已移除相关依赖
