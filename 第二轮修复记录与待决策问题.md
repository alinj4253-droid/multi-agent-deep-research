# 第二轮修复记录与待决策问题

> 修复时间：2026-09-20（下午～晚间）
> 承接文档：《审查记录与待决策问题.md》（第一轮）
> 本轮范围：你反馈的 6 项测试问题全部处理完毕，并清理了你确认要删的遗留内容
> 测试基线：**106 passed**（第一轮 51 → 本轮 106，新增 55 条，零回归）
> 改动规模：修改 19 个文件、删除 32 个、新增 3 个

---

## 一、你反馈的 6 项问题：逐条对照结果

### 问题 1 · 两个前端只留一个 ✅ 已处理，但**结论与你的判断相反**

你的原话是"应该是那个 demo 是可以删除的"。实际调查后正好相反：

| 对比项 | `frontend-demo/` | `deepsearch-agents/frontend/` |
|---|---|---|
| 当前是否在跑 | ✅ 运行在 5173 | ❌ 未运行 |
| 依赖是否安装 | ✅ 128 个包 | ❌ 无 node_modules |
| git 提交次数 | **5 次**（持续迭代） | 1 次（仅初始化） |
| 后端端口配置 | ✅ 8001（正确） | ❌ 8000（旧端口） |
| 包管理器 | npm（可用） | pnpm（未安装） |
| 侧边栏助手列表 | ✅ 网络/数据/学术，与后端一致 | ❌ 列着「数据库查询助手」「RAGFlow 助手」，**后端根本没有这两个** |

`frontend/` 是上游原版残留（Ant Design 版），名字带 "demo" 的才是你这几轮一直在改的主力前端。

**已按你确认的选择执行**：保留 `frontend-demo/`，`deepsearch-agents/frontend/` 整体送入回收站（git 历史仍可恢复）。

> ⚠️ 注意：你原以为 demo 版功能缺失需要重写，实际 `frontend/` 里确实有更成熟的多轮对话实现，
> 我把它作为参考重新在 `frontend-demo/` 上实现了（见问题 2），不是简单搬代码——因为两边技术栈不同
> （Tailwind vs Ant Design + 独立 CSS），直接搬会引入未安装的依赖。

---

### 问题 2 · 多轮对话丢失（第二个问题顶掉第一个）✅ 已修复

**根因**（两处叠加）：

1. `frontend-demo/src/hooks/useDeepAgentSession.ts` 的 `submitTask` 每次提交都执行
   `setEvents([])` 和 `setResult("")`，把上一轮的状态清空；
2. `App.tsx` 用单个 `sentQuery` 字符串保存问题，只可能记住最后一问。

**修复**：`App.tsx` 改为 `turns: ChatTurn[]` 数组按轮累积，每轮独立保存
`query / events / files / result / error`。用 `liveTurnIdRef` 标记当前实时轮次，
WebSocket 推送的事件只写入这一轮，**不会污染已恢复的历史轮次**。

界面上第二轮起会显示「第 N 轮」分隔线，每轮的答案、产物文件、错误提示都独立成块。

**附带修复**：某轮若在完成前被取消（同一 thread_id 只保留一个活跃任务，后端会取消旧任务），
该轮 `answer` 为空，界面显示「该轮未产生最终回答（可能在完成前被取消，或执行出错）」，
不再是空白气泡。这正是你库里那个自动驾驶会话第一轮的实际情况。

---

### 问题 3 · 前端没有上传文件入口 ✅ 已补齐

后端 `/api/upload`、hook 的 `uploadFiles` 早就写好了，只差界面。

**已实现**：输入框左侧新增 📎 附件按钮（支持多选），已上传文件以蓝色标签显示在输入框上方，
Agent 可通过 `read_file_content` 工具读取。支持 `.md .txt .docx .pdf .xlsx .csv`。

**同时修正了那个误导性的示例按钮**：原按钮文案「分析上传的数据集，做性能对比」点了之后
没有任何文件可分析。现在改为「上传数据集做统计分析」，点击后**直接唤起文件选择框**，
文案与实际能力一致。

**顺带加固后端**：`/api/upload` 的 `thread_id` 会拼进目录名，新增白名单校验
（`[A-Za-z0-9_\-]{1,64}`），堵住 `../../etc` 这类穿越；文件落盘改到线程池执行，避免大文件阻塞事件循环。

---

### 问题 4 · 问 2026 年进展却返回 2023-2024 论文 ✅ 已修复（本轮最有价值的一处）

**根因有三层**，全部定位并修复：

**① 排序权重错**（主因）
`academic_sources.py` 的 `merge_and_rank()` 排序键是
`(citation_count, year, len(title))` —— **引用数排在年份前面**。
高被引需要时间积累，所以问"最新进展"必然被 2023-2024 的经典论文压顶，这是必然结果而非偶发。

修复：改为双模式排序
- 传了 `year_from`（时效性需求）→ **年份优先**，同年内再比引用数
- 没传（综述/发展脉络需求）→ 保持引用数优先，向后兼容

现有测试 `test_rank_by_citation_desc` 固化的正是旧行为，新增 8 条测试覆盖新行为，旧的仍全过。

**② arXiv 通道年份过滤位置错**
`search_arxiv()` 先按相关性取 top-5，再在本地过滤年份。指定较新年份时，
取回的 5 条里可能一篇都不符合，直接返回空。

修复：把年份下推到 API 查询参数 `submittedDate:[202601010000 TO 999912312359]`，
本地过滤保留作安全网。

**③ 提示词没要求模型用 `year_from`**
`prompts.yml` 里只有一句"需要限定近年文献时用 year_from"，太弱，模型经常忽略。

修复：新增专门的纪律条款并加粗标注「最容易漏的一条」，给出明确决策规则：
- 用户写了年份 → `year_from` 等于该年份
- 只说"最新/近年" → `year_from` 取当前年份减 2
- 只有"发展脉络/综述/奠基工作"才不传

工具 docstring 同步强化（docstring 会被暴露给模型作为参数说明）。

**实测对比**（同一查询：自动驾驶多模态数据融合）：

| | 修复前 | 修复后 |
|---|---|---|
| 返回年份分布 | 2012、2020×3、2021、2023、2024、2025 | **2024×2、2025×4、2026×2** |
| 排在首位的论文 | 2012 年 KITTI（引用 12122） | 2026 年不确定性感知证据融合 |

端到端跑你的原话场景，最终报告标题是「自动驾驶多模态数据融合：2023–2026 进展与趋势」，
表格里 8 篇论文 2025 年 4 篇、2026 年 4 篇，且每篇都标注了年份和来源链接。

---

### 问题 5 · 历史会话列表写死「暂无」，SQLite 里其实有数据 ✅ 已实现

**新增后端 `app/api/threads.py`**（全新模块）+ 两个接口：

| 接口 | 作用 |
|---|---|
| `GET /api/threads?limit=30` | 会话列表：标题、最近活动时间、步数、产物目录 |
| `GET /api/threads/{thread_id}` | 单会话多轮问答详情，点击侧边栏后恢复 |

**实现要点**（都经过真实库验证，不是猜的）：

1. **只读连接**：用 `file:...?mode=ro` URI 打开，绝不干扰主智能体持有的 `AsyncSqliteSaver` 写连接；
2. **时间排序**：库里没有时间列，但 `checkpoint_id` 是 **UUID6**（时间有序）。
   实测 865 个 id，字符串序与时间序**完全一致**，因此 `MAX(checkpoint_id)` 就是最近活动时间，
   并能还原出精确到微秒的时间戳；
3. **标题提取**：`writes.value` 是 msgpack，用 langgraph 自带的 `JsonPlusSerializer` 解码，
   取首条 `HumanMessage`。库里 47 个会话全部成功提取；
4. **剥离噪声**：`main_agent` 会往用户消息尾部拼「【工作环境指令】工作目录: ... 规则: ...」，
   不剥离的话侧边栏会显示一大段路径。`strip_workspace_instruction()` 处理；
5. **过滤测试会话**：`e2e-` / `diag-` / `verify-` / `test-` / `smoke-` 前缀的自动化会话
   默认不展示（47 个会话 → 侧边栏 8 个真实会话）；
6. **线程池执行**：`run_in_threadpool`，不阻塞事件循环；
7. **降级**：库文件缺失返回空列表而非 500；会话不存在返回 404。

**前端侧边栏**：渲染真实会话列表，显示标题（两行截断）、相对时间、步数，当前会话高亮标「当前」，
带「刷新」按钮。点击后：
- 调 `/api/threads/{id}` 还原该会话全部问答轮次
- 切换 `thread_id`，WebSocket 重连到该会话，**后续提问接着这条上下文继续**（后端 checkpointer 按 thread_id 续接记忆）
- 接管该会话的产物目录，恢复之前生成的 PDF/PNG 下载列表

**前端刷新页面也能恢复**：`thread_id` 存在 localStorage，刷新后仍在同一会话。

**会话详情还原的两道关键判别**（对真实库验证过，不是拍脑袋）：

| 难点 | 判别依据 | 验证结果 |
|---|---|---|
| 怎么区分「用户真实输入」和「子智能体派单消息」 | 真实输入被拼了【工作环境指令】标记，派单消息没有 | 6 个会话抽样，派单消息**全部**不含标记 |
| 怎么排除子智能体内部对话 | langgraph 用 `checkpoint_ns` 区分，主线程恒为空串 | `checkpoint_ns=''` 精确还原主线，序列完全符合预期 |
| 怎么找「最终答案」而非中间过程 | 取下一条真实输入前、最后一条「无 tool_calls 且有正文」的 ai 消息 | 带 tool_calls 的中间消息被正确跳过 |

---

### 问题 6 · ragflow-sdk 声明了依赖但没装，一 import 就报错 ✅ 已修复

采用你列的三个选项里的**②延迟导入**（保留能力、不引入未验证的依赖）：

- 新增 `RagflowNotAvailable` 异常和 `get_ragflow_client()` 工厂函数
- 把 `from ragflow_sdk import RAGFlow` 从模块顶部移进函数内部
- 三个文件改造：`rag_config.py`、`ragflow_tools.py`、`knowledge_demo.py`

**效果实测**：

```
模块导入（SDK 未安装）  →  三个模块全部 import 成功
调用工具              →  返回「RAGFlow 未配置：请在 .env 中设置 RAGFLOW_API_KEY
                          和 RAGFLOW_API_URL 后再调用知识库工具。」（不抛异常）
主链路                →  完全不受牵连，正常导入运行
```

**依赖声明同步修正**（原来两处都写死了 `ragflow-sdk>=0.1.0`，与环境不符）：
- `pyproject.toml`：移到 `[project.optional-dependencies]` 的 `ragflow` 组
- `requirements.txt`：改为注释状态，附启用说明
- 需要时用 `pip install -e ".[ragflow]"` 或直接 `pip install ragflow-sdk`

---

## 二、你确认要删的遗留内容 ✅ 全部清理（均送回收站，可恢复）

| # | 目标 | 原因 | 处置 |
|---|------|------|------|
| 1 | `deepsearch-agents/frontend/` | 上游原版前端，未被使用（见问题 1） | 送回收站 |
| 2 | `deepsearch-agents/docker/` | 遗留 MySQL（compose + 43MB 假数据 SQL），现用 SQLite | 送回收站 |
| 3 | `_tmp_diag3.log`、`_tmp_diag_data.log` | 一次性诊断产物 | 送回收站 |
| 4 | `uv.lock` | 仍锁着已移除的 `tavily-python==0.7.24`，把 `ragflow-sdk` 当必需依赖；`uv` 未安装，项目实际用 pip + venv | 送回收站 |
| 5 | `frontend-demo/src/App.css` | Vite 模板残留，全项目零引用（样式走 Tailwind） | 送回收站 |
| 6 | `.pre-commit-config.yaml` | 引用了未安装的 `uv run --frozen`，且 `--frozen` 要读已删的锁文件，启用必然失败 | 改为直接调用 `ruff` |
| 7 | `ruff` 依赖缺失 | pre-commit 引用但 dev 依赖里没有 | 补进 `pyproject.toml` 与 `requirements.txt` |

**删除前已做的安全检查**：
- 全仓库 grep 引用关系，确认这些目标在**代码层面零引用**（只在上游教程文档里被提到）
- 特别注意 `searxng/docker-compose.yml` 是另一个**在用**文件，与 `deepsearch-agents/docker/` 无关，未误删
- 全部走 Windows 回收站，不做永久删除

**连带修正的文档不一致**（删了东西但文档还在指它，会误导后来人）：
- `deepsearch-agents/README.md` 的「📁 项目结构」树：移除 `frontend/`、`docker/`、`uv.lock` 条目，
  补上新增的 `app/api/threads.py`、`checkpoints.db`、`scripts/`，并说明前端实际在 `../frontend-demo/`
- 同文件顶部免责声明：新增⚠️提示，说明教程正文涉及的 `frontend/`、`docker/`、`uv sync` 已不适用
- 本地运行指南的安装命令：`uv sync` → `pip install -r requirements.txt`
- **端口笔误 5174 → 5173**：`项目进度总览与收尾计划.md`（2 处）、`第四阶段进度总结与后续规划.md`（1 处）
  > 另有 2 处 5174 出现在《审查记录》和《改造方案》里，但那两处是在**描述这个问题本身**，属正确内容，未改

---

## 三、顺手修的其他问题

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `app/api/server.py` | CORS 是 `allow_origins=["*"]` + `allow_credentials=True`。这个组合在浏览器规范下**不生效**（带凭证时禁止通配源），等于跨域配置形同虚设 | 收敛为 `http://localhost:5173` + `127.0.0.1:5173`，支持 `CORS_ORIGINS` 环境变量扩展 |
| 2 | `app/utils/path_utils.py` | `updated/` 分支用 `Path(x).resolve()`，基准是进程 CWD（`deepsearch-agents/`），但上传文件实际在 `app/updated/`，模型传相对路径会解析到不存在的位置 | 改为以 `app` 目录为基准拼接 |
| 3 | `app/api/server.py` | `/api/upload` 的 `shutil.copyfileobj`、`/api/files` 的 `rglob` 遍历都是事件循环内的**同步阻塞 IO**，大文件或产物多时会卡住整个服务 | 用 `run_in_threadpool` 移出事件循环 |
| 4 | `app/api/server.py` | `/api/files` 里遗留 `print(f"[DEBUG] ...")` 调试输出 | 清理并统一日志前缀为 `[Files]` |
| 5 | `frontend-demo/src/App.tsx` | 输入框是单行 `<input>`，长研究任务没法换行编辑 | 改为自适应 `<textarea>`，Enter 发送、Shift+Enter 换行 |
| 6 | `frontend-demo/src/App.tsx` | 任务执行中无法取消（hook 有 `cancelCurrentTask` 但界面没用） | 执行中时发送按钮变为红色「取消」按钮 |
| 7 | `frontend-demo/src/App.tsx` | 代码块 `overflow-x-auto` 但没有 `whitespace-pre-wrap`，长代码会横向溢出 | 补 `whitespace-pre-wrap` |
| 8 | `frontend-demo/src/App.tsx` | 顶部看不到当前会话 ID，排查问题时无法定位 | 标题旁显示 `thread_id` 前 8 位（带完整值 title 提示） |

---

## 四、测试与验证

### 新增测试 55 条

| 文件 | 条数 | 覆盖 |
|---|---|---|
| `tests/test_threads.py` | 32 | 纯函数（标题剥离/截断/测试会话判定/UUID6 时间解析）、`list_threads`（过滤/排序/limit 钳制/库缺失降级/session_path 存在性）、`build_turns`（多轮还原/派单排除/中间过程跳过/子智能体命名空间隔离/重复消息去重/多模态内容展平）、`get_thread_detail`、`path_utils` updated 分支基准 |
| `tests/test_api_endpoints.py` | 15 | `/api/threads` 结构与参数透传、异常降级 500、`/api/threads/{id}` 404、上传 thread_id 白名单（穿越/空值/超长）、文件名穿越剥离、`/api/files` 403 越权与 404、`/api/download` 越权 |
| `tests/test_academic_sources.py` | +8 | 年份优先排序（低引用的新论文压过高引用的老论文）、未传 `year_from` 时保持引用优先、同年按引用、年份缺失排最后、`academic_search` 透传 `year_from`、arXiv `submittedDate` 下推与未限定时不加 |

**测试设计原则**：不依赖外网、不依赖大模型、不依赖那个 17MB 的真实库。
用合成 SQLite 库（schema 与 langgraph `AsyncSqliteSaver` **逐字段一致**），
消息体用 langgraph 自带 `JsonPlusSerializer` 序列化为 msgpack，与真实库同构。

**全量结果**：`106 passed`（第一轮 51 → 59 → 106，零回归）
前端：`tsc -b` 零错误、`vite build` 成功（exit 0）

### 实测验证（真实链路，非 mock）

| 项目 | 结果 |
|------|------|
| 后端启动 | ✅ 8001 监听，`/docs` HTTP 200 |
| OpenAPI 路由 | ✅ 7 条全部注册，含新增的 `/api/threads` 与 `/api/threads/{thread_id}` |
| `/api/threads` | ✅ 返回 8 个真实会话，中文标题正常（此前 PowerShell 显示乱码是控制台解码问题，接口返回字节正确） |
| `/api/threads/{id}` | ✅ 还原 3DGS PDF 会话 1 轮问答；不存在的会话返回 404 |
| `/api/files` | ✅ 会话目录列出 `3DGS发展历程.pdf`(11791B) + `.md`(6096B)；访问 `C:\Windows` 返回 403 |
| CORS 预检 | ✅ `Origin: localhost:5173` 放行并回显；`Origin: evil.example.com` **拒绝**（Allow-Origin: None） |
| WebSocket | ✅ 连接成功，ping → pong 正常 |
| 前端页面 | ✅ HTTP 200，标题正确，`App.tsx` 编译产物 97723 字节，四项改造代码全部在内 |
| E2E 常识直答 | ✅ 1.2s，零工具调用，直接作答 |
| E2E 学术时效性 | ✅ 45.5s，路由到学术文献助手，`year_from=2023`，报告 8 篇论文全为 2025-2026 年 |
| RAGFlow 延迟导入 | ✅ 三个模块 import 成功，调用返回中文配置提示，主链路不受牵连 |

---

## 五、⚠️ 需要你决策的问题（本轮只记录，未动）

### P1 — 建议处理，但改动涉及取舍

**1. `docs/knowledge_base/` 下 5 个 PDF 共 21MB，已无代码引用**

```
电商行业/2026数字人电商直播白皮书.pdf              5.7MB
电商行业/全球电商行业AI应用研究报告2025.pdf        6.4MB
金融行业/贝莱德BlackRock2026年全球投资展望报告.pdf 1.5MB
金融行业/长江商学院2025年投资者情绪问卷调查.pdf    5.8MB
金融行业/中国人民银行2025年第四季度货币政策报告.pdf 1.6MB
```

这些原本是给 **RAGFlow 知识库**用的示例文档。RAGFlow 现在是可选能力且未安装 SDK，
所以这批 PDF 处于「留着占空间、删了将来接 RAGFlow 又要重找」的状态。
**需要你定**：删除 / 保留 / 移到 `legacy/` 归档？

**2. `app/output/session_*` 会话产物持续累积，无清理策略**

现在 7 个会话目录。每次跑任务都会新建一个，长期使用会无限增长
（`checkpoints.db` 已经 17MB + 4MB WAL 了）。
**需要你定**：是否加「保留最近 N 个会话」的清理策略？还是保持现状手动清？

**3. `examples/` 目录（15 个上游教程脚本）中有 2 个引用已移除的 Tavily**

`examples/1-deep-agent-quickstart-search.py` 和 `examples/2-deep-agent-streaming-chunks.py`
里有 `from tavily import TavilyClient`，跑起来必然 ImportError
（其余 13 个脚本不涉及 Tavily，可正常运行）。这些是上游 DeepAgents 教程示例，不是本项目代码，
`README.md` 顶部已声明以本地指南为准。
**需要你定**：删除这 2 个 / 整个 `examples/` 删除 / 保留作对照学习？

**4. `deepsearch-agents/README.md` 的「快速开始」整章仍是上游原文**

L283 之后那一大段（准备环境要求 uv/pnpm/Tavily/RAGFlow、启动 MySQL 教学库、
配置 RAGFlow 知识库、8000 端口）与实际项目**完全不符**，虽然顶部有免责声明，
但篇幅很长（约 160 行），容易误照着执行。
**需要你定**：整章删除 / 精简为「上游原始设计说明」附录 / 保持现状？

### P2 — 观察项，暂不影响使用

**5. arXiv 偶发超时无重试**
三源容错已能用 OpenAlex/Crossref 兜底，本轮实测三源全部可用、无失败源。
若要更稳，可给 arXiv 单源加 1 次重试。

**6. `searxng/settings.yml` 里 `secret_key` 明文提交**
本地单用户实例风险很低。若将来要公开仓库或分享，建议改读环境变量并更换 key。

**7. 前端 `api.ts` 与 `types.ts` 都定义了 `OutputFile` 接口（重复）**
两份定义目前字段一致，所以没出问题，但改一处忘另一处会导致类型不一致。
建议统一到 `types.ts`。

**8. 前端 `frontend-demo/src/App.css` 已删，但 `src/assets/` 下还有模板残留图片**
`hero.png`(13KB)、`react.svg`(4KB)、`vite.svg`(9KB) 共约 25KB，是 Vite 脚手架默认资源。
已逐个核实：三个文件在 `src/` 与 `index.html` 中**全部零引用**。

**9. `frontend-demo/dist/` 是构建产物，已被 .gitignore 忽略但仍在磁盘上**
本轮验证构建时生成的，可随时删。

---

## 六、当前运行状态与人工测试指引

| 组件 | 状态 | 地址 |
|------|------|------|
| SearXNG | ✅ 运行中（Up 6 hours） | WSL2 `Ubuntu-20.04` Docker 容器，容器内 8888 |
| WSL 保活 | ✅ 2 个常驻进程在跑 | `keep_wsl_alive.sh` |
| 后端 | ✅ 已用新代码重启 | **http://localhost:8001** |
| 接口文档 | ✅ | **http://localhost:8001/docs** |
| 前端 | ✅ 已用新代码重启 | **http://localhost:5173** |

### 建议的人工测试路径（重点验证本轮修复的 4 处）

**① 多轮对话不丢历史（问题 2）**
1. 打开 http://localhost:5173 ，左下角应为绿色「已连接」
2. 问第一个问题：「用一句话解释什么是光合作用」→ 等它答完
3. **不清空页面**，接着问第二个问题：「检索 Python 3.13 的主要新特性」
4. ✅ 预期：第一个问答**仍完整显示在上方**，中间有「第 2 轮」分隔线

**② 文件上传（问题 3、5）**
1. 点输入框左侧 📎 按钮，选一个 `.md` / `.csv` / `.pdf` 文件
2. ✅ 预期：文件名以蓝色标签显示在输入框上方
3. 提问「分析我上传的文件，总结要点」
4. ✅ 预期：事件流里出现「文件内容读取工具」，回答基于文件真实内容

**③ 历史会话侧边栏（问题 5）**
1. ✅ 预期：左侧「历史会话」下**不再是「暂无」**，应显示约 8 条真实会话
   （含「检索2026年自动驾驶方面的关于多模态数据融合…」「写一份3DGS发展历程的PDF给我…」等），
   每条带时间和步数
2. 点其中一条（建议点「写一份3DGS发展历程的PDF」）
3. ✅ 预期：右侧还原该会话的问答内容，下方出现 `3DGS发展历程.pdf` / `.md` 可下载
4. 点「刷新」按钮 → 列表应重新加载
5. 点「+ 新建研究任务」→ 对话清空，thread_id 变化

**④ 学术检索时效性（问题 4，本轮最有价值的修复）**
1. 直接复现你原来的问题：「检索 2026 年自动驾驶方向数据融合方面的最新进展和趋势」
2. ✅ 预期：事件流出现「学术文献检索」且参数里带 `year_from`
3. ✅ 预期：报告表格里的论文**主要是 2025-2026 年**，每篇标注年份，
   而不是像上次那样给 2023-2024 的老论文

**⑤ 取消任务**
1. 提交一个耗时任务（如学术检索），执行中发送按钮变为红色「取消」
2. 点取消 → ✅ 预期：显示「任务已取消」，该轮标注未产生最终回答

**⑥ 刷新恢复**
1. 在某个会话里问完一个问题，按 F5 刷新页面
2. ✅ 预期：仍在同一会话（thread_id 不变），可继续提问并接上上下文

> 注：每次「新建研究任务」会生成新 thread_id；检索预算按 thread 计数（网页 3 次、学术 2 次）。
> 点击历史会话恢复后，后续提问会**接着那条会话的上下文**继续，而不是全新开始。
