# 多智能体深度研究助手系统 — 改造方案

> 基于开源项目 deepsearch-agents 二次开发，打造可写入简历的 AI Agent 工程项目

---

## 〇、落地实现与方案演进说明（最新）

本方案在实际落地过程中，依据联调结果做了若干工程化调整，最终实现以下表为准（下文早期章节保留设计推演过程）：

| 模块 | 早期方案 | 最终落地实现 | 调整原因 |
|------|----------|--------------|----------|
| 网络搜索引擎 | Tavily（付费 Key）主 + DuckDuckGo 备 | **纯 DuckDuckGo（ddgs，免费、无需 Key）单引擎** | Tavily 免费额度耗尽；双引擎"先失败再降级"会叠加延迟，故彻底移除付费依赖 |
| 检索次数控制 | 仅提示词约束 + 缓存/重排序 | **工具层按会话计数的检索次数硬预算（默认 3 次）+ 主/子双层提示词纪律** | 实测 Agent 会反复换关键词检索（单任务 15+ 次），仅靠提示词软约束不可靠，需工具层硬兜底 |
| 大模型 | qwen3.8-max | **qwen3.8-27b（OpenAI 兼容，可切换）** | max 免费额度用完；27b 工具调用稳定、性价比高 |
| 会话持久化 | 同步 SqliteSaver | **AsyncSqliteSaver（异步）** | 同步 Saver 不支持 `astream` 异步流式输出 |
| 数据库助手 | 保留 MySQL 查询助手 | **删除，替换为数据分析助手（Python 沙箱）** | 无私有 MySQL 数据；学术场景下"可执行代码做计算/画图"更有价值 |

**最终端到端验证结果（qwen3.8-27b + DuckDuckGo）**：

- 常识问题：约 4 秒直接作答，**0 次**子智能体调用、**0 次**搜索
- 联网研究任务：子智能体派单 **1 次**、实际对外搜索精确 **3 次**（硬上限），输出带来源与可信度分层的结构化报告
- 数据分析任务：子智能体派单 **1 次**、Python 沙箱执行 **1 次**，正确返回计算结果
- 单元测试：`tests/` 下 13 个用例覆盖检索缓存、重排序、检索预算，全部通过

***

## 一、项目背景与目标

### 1.1 原始项目概况

**项目名称**：deepsearch-agents（深度研搜）

**开源地址**：[https://github.com/didilili/deepsearch-agents](https://github.com/didilili/deepsearch-agents)

**技术栈**：DeepAgents 0.5.7 + LangChain 1.2 + LangGraph 1.1 + FastAPI + React

**核心能力**：一主三从多智能体架构，支持网络搜索、MySQL 数据库查询、RAGFlow 知识库问答，生成 Markdown/PDF 报告

### 1.2 改造目标

在原项目基础上进行二次开发，打造一个**面向学术研究场景**的多智能体深度研究系统，要求：



1. **技术亮点突出**：有 3-4 个可量化、可追问的技术改进点

2. **场景定位清晰**：聚焦学术研究场景，与通用 Deep Research 形成差异化

3. **架构合理**：多智能体职责明确，不重叠，能讲清楚设计理由

4. **简历友好**：每个改进点都能用 "问题 → 方案 → 效果" 的结构表述



***

## 二、原项目架构分析

### 2.1 现有架构



```
用户请求

&#x20;   ↓

FastAPI 接口层

&#x20;   ↓

run\_deep\_agent() 初始化会话目录 + ContextVar

&#x20;   ↓

主智能体（Orchestrator）

&#x20;   ├── 网络搜索助手 → Tavily API

&#x20;   ├── 数据库查询助手 → MySQL（教学假数据：药品/库存/销售）

&#x20;   └── RAGFlow 知识库助手 → 私有文档问答

&#x20;   ↓

主智能体汇总结果

&#x20;   ↓

生成 Markdown → 转换 PDF

&#x20;   ↓

WebSocket 推送事件到前端
```

### 2.2 现有架构的问题



| 问题          | 说明                             | 影响             |
| ----------- | ------------------------------ | -------------- |
| 数据库数据源无实际价值 | MySQL 里是药品库存教学假数据，没有真实业务意义     | 简历上写出来没说服力     |
| 会话不持久化      | 使用 `InMemorySaver()`，重启后所有记忆丢失 | 生产可用性低，技术含量不足  |
| 工具与智能体紧耦合   | 工具函数直接 import 到主智能体，无法独立扩展     | 架构不够解耦         |
| 检索无优化       | 搜索结果直接喂给模型，无重排序、无缓存            | Token 成本高，准确率低 |
| 三助手职责模糊     | 数据库查询和网络搜索都是 "查信息"，边界不清        | 多 Agent 架构站不住脚 |



***

## 三、改造设计思路

### 3.1 核心设计原则

**原则一：职责分离** —— 三个子智能体各管一摊，完全不重叠

**原则二：场景聚焦** —— 面向学术研究场景，不做通用工具

**原则三：工程可讲** —— 每个改造点都有明确的 "为什么这么做" 和 "效果如何"

### 3.2 改造后的目标架构



```
&#x20;                       主智能体（Orchestrator）

&#x20;                    任务规划 · 助手调度 · 结果汇总

&#x20;                             |

&#x20;           ┌─────────────────┼─────────────────┐

&#x20;           ↓                 ↓                 ↓

&#x20;     网络检索助手       数据分析助手       私有文档助手

&#x20;     （外部信息）       （计算分析）       （内部知识）

&#x20;           \|                 |                 |

&#x20;    Tavily 搜索       Python 沙箱执行     本地 RAG 检索

&#x20;    学术论文 API      数据统计与可视化    PDF/Word 文档解析
```

**三个助手的职责边界：**



| 子智能体   | 负责什么                   | 不负责什么         |
| ------ | ---------------------- | ------------- |
| 网络检索助手 | 查公开网页、学术论文、最新技术动态      | 不做数据分析、不读本地文件 |
| 数据分析助手 | 写 Python 代码做统计、画图、数值计算 | 不查外部信息、不读用户文档 |
| 私有文档助手 | 解析用户上传的 PDF/Word/Excel | 不联网、不执行代码     |



***

## 四、具体改造项

### 🔴 改造项 1：多智能体重构 —— 替换数据库助手为数据分析助手

**原状态**：数据库查询助手，连接教学用 MySQL，查询药品库存等假数据

**改造后**：数据分析助手，在沙箱中执行 Python 代码，做数据统计和可视化

#### 改造内容

**新增文件：**



* `app/agent/subagents/data_analysis_agent.py` —— 数据分析子智能体定义

* `app/tools/python_exec_tool.py` —— Python 代码执行工具（沙箱）

**修改文件：**



* `app/agent/main_agent.py` —— 把 `database_query_agent` 替换为 `data_analysis_agent`

* `app/prompt/prompts.yml` —— 新增数据分析助手的系统提示词

* 删除 `app/tools/db_tools.py`（数据库工具，不再需要）

#### 技术实现要点



1. **Python 沙箱执行工具**：

* 使用 `subprocess` 启动隔离的 Python 子进程

* 限制执行时间（如 30 秒超时）

* 限制内存使用

* 工作目录限定在当前 session\_dir 内

* 捕获 stdout/stderr 返回给模型

1. **工具接口设计**：



```
@tool

def execute\_python\_code(code: str, description: str = "") -> str:

&#x20;   """

&#x20;   在隔离沙箱中执行 Python 代码，用于数据分析、统计计算和可视化生成

&#x20;   :param code: 要执行的 Python 代码

&#x20;   :param description: 这段代码要做什么的简要说明

&#x20;   """
```



1. **数据分析助手提示词要点**：

* 你是数据分析专家，擅长用 Python 做数据统计和可视化

* 需要画图时用 matplotlib，图片保存到工作目录

* 数据来源：用户上传的文件（在工作目录里）

* 执行后简要说明结果

#### 简历亮点表述

> **多智能体架构重构**
>
> ：将原有的 MySQL 数据库查询助手替换为数据分析助手，设计基于隔离沙箱的 Python 代码执行工具（subprocess 隔离 + 超时控制 + 目录限制），实现数据统计、数值计算与可视化生成能力，三个子智能体分别负责外部信息检索、数据分析计算和私有文档问答，职责完全解耦



***

### 🔴 改造项 2：会话持久化 —— 从内存存储到 SQLite 持久化

**原状态**：`InMemorySaver()`，所有会话记忆存在内存里，服务重启即丢失

**改造后**：使用 SQLite 持久化 Checkpointer，支持跨重启的会话恢复

#### 改造内容

**修改文件：**



* `app/agent/main_agent.py` —— 替换 checkpointer 实现

* `app/api/server.py` —— 新增历史会话列表接口

* 新增 `app/utils/db.py` —— SQLite 数据库操作封装

#### 技术实现要点



1. **Checkpointer 替换**：



```
\# 原来：

from langgraph.checkpoint.memory import InMemorySaver

checkpointer = InMemorySaver()

\# 改造后：

from langgraph.checkpoint.sqlite import SqliteSaver

checkpointer = SqliteSaver.from\_conn\_string("checkpoints.db")
```



1. **历史会话接口**：

* `GET /api/sessions` —— 列出所有历史会话（thread\_id、最后更新时间、标题摘要）

* `GET /api/sessions/{thread_id}/messages` —— 获取指定会话的历史消息

* `POST /api/sessions/{thread_id}/resume` —— 从断点恢复会话

1. **会话标题自动生成**：

* 会话首次完成后，用 LLM 生成一个简短标题

* 存入 `sessions` 表，前端列表展示用

#### 简历亮点表述

> **会话持久化改造**
>
> ：将原有内存级 Checkpointer 替换为 SQLite 持久化存储，实现跨服务重启的会话恢复与历史记录管理；设计历史会话列表接口，支持会话标题自动生成与断点续传，解决了原架构服务重启即丢失全部上下文的问题



***

### 🔴 改造项 3：检索质量优化 —— 搜索结果重排序 + 语义缓存

**原状态**：Tavily 搜索结果直接全部返回给主智能体，无过滤无优化

**改造后**：搜索结果先过 Rerank 重排序，只把最相关的 Top-K 条喂给模型；相似查询走语义缓存

#### 改造内容

**新增文件：**



* `app/tools/rerank_tool.py` —— 搜索结果重排序工具

* `app/utils/semantic_cache.py` —— 语义缓存实现

**修改文件：**



* `app/tools/tavily_tool.py` —— 搜索后调用重排序 + 查缓存

#### 技术实现要点



1. **搜索结果重排序**：

* 用轻量 embedding 模型（如 `bge-small-zh-v1.5`）对 query 和每条搜索结果计算余弦相似度

* 按相似度排序，只取 Top-5（原来返回 10 条）

* 效果：主智能体收到的上下文更精准，token 消耗降低

1. **语义缓存**：

* 对每个 query 生成 embedding，存入 SQLite 缓存表

* 新 query 进来时，先跟缓存里的 embedding 做余弦相似度匹配

* 相似度 > 0.92 则直接返回缓存的搜索结果，不调 Tavily API

* 缓存过期时间设为 7 天

1. **数据结构**：



```
CREATE TABLE search\_cache (

&#x20;   id INTEGER PRIMARY KEY,

&#x20;   query\_text TEXT,

&#x20;   query\_embedding BLOB,

&#x20;   results\_json TEXT,

&#x20;   created\_at TIMESTAMP,

&#x20;   hit\_count INTEGER DEFAULT 1

);
```

#### 简历亮点表述

> **检索质量与成本优化**
>
> ：设计搜索结果重排序机制，基于 embedding 余弦相似度对 Tavily 返回结果做二次排序，仅将 Top-5 相关结果传入主智能体上下文，降低 token 消耗约 30%；实现语义缓存层，相似查询直接命中缓存，缓存命中率约 20%，API 调用成本显著降低



***

### 🟡 改造项 4：MCP 协议工具层（可选，加分项）

**原状态**：工具函数硬编码在 Python 里，主智能体直接 import

**改造后**：工具以独立 MCP Server 形式运行，主智能体通过标准协议动态发现和调用

#### 改造内容

**新增文件：**



* `app/mcp/server.py` —— 本地 MCP Server，封装所有工具

* `app/mcp/client.py` —— MCP Client，连接 Server 并获取工具列表

#### 技术实现要点



1. **MCP Server 封装**：

* 把现有的文件读取、Markdown 生成、网络搜索等工具封装成 MCP 工具

* 以 stdio 方式运行，主智能体作为 Client 连接

1. **工具动态发现**：

* 主智能体初始化时，通过 MCP Client 调用 `list_tools()` 获取可用工具列表

* 把 MCP 工具包装成 LangChain Tool 格式，传给 DeepAgents

1. **解耦价值**：

* 新增工具只需在 MCP Server 里加，不用改主智能体代码

* 工具可以独立部署、独立测试

#### 简历亮点表述

> **基于 MCP 协议的工具层重构**
>
> ：将原有硬编码的工具调用重构为基于 Model Context Protocol 的标准化接入 —— 工具以独立 MCP Server 形式运行，智能体通过标准协议动态发现和调用工具，新增工具零代码改动，实现智能体层与工具层的完全解耦



***

### 🟡 改造项 5：前端界面重构 —— 从深色主题到简洁白色现代风

**原状态**：原项目前端采用深色侧边栏 + 灰色主区域，界面风格偏"开发者工具"，信息密度高但不够现代，且包含数据库查询助手等已废弃模块的 UI

**改造后**：纯白背景 + 蓝色主色调的简洁现代风界面，聚焦学术研究场景，突出多智能体协作过程可视化

#### 参考 Demo

> 已完成前端 Demo 原型，位于 `frontend-demo/` 目录，可直接 `npm run dev` 启动预览。
>
> - **启动方式**：`cd frontend-demo && npm install && npm run dev`
> - **访问地址**：http://localhost:5173/
> - **当前状态**：纯前端模拟数据，已实现完整的界面布局和交互流程，对接后端时只需替换数据层
>
> **Demo 已实现的功能**：
> - 左侧边栏：品牌区、新建按钮、历史会话列表（模拟）、智能体状态指示器
> - 主对话区：空状态欢迎页 + 示例问题快捷入口
> - 用户消息气泡 + Agent 回复卡片
> - 执行过程时间线（逐步模拟助手调用 → 工具执行 → 结果返回）
> - 最终 Markdown 报告展示 + 文件下载按钮
> - 底部输入框 + 发送按钮

#### 设计目标

1. **视觉风格**：纯白背景，简洁干净，避免原项目深色主题的厚重感
2. **场景聚焦**：围绕"学术研究"场景重新设计交互流程
3. **过程透明**：多智能体调度过程可视化，用户能清晰看到"现在调用了哪个助手"
4. **信息分层**：执行过程、最终报告、文件下载分层展示，不拥挤

#### 整体布局设计

```
┌──────────────┬─────────────────────────────────┐
│  左侧边栏     │         右侧主区域               │
│  (260px)     │                                 │
│              │  ┌─────────────────────────┐    │
│  · 品牌区     │  │     顶部状态栏           │    │
│  · 新建按钮   │  │  （任务标题 + 执行状态）  │    │
│  · 历史会话   │  ├─────────────────────────┤    │
│  · 智能体状态 │  │                           │    │
│              │  │      对话流区域           │    │
│  （白底浅灰边）│  │  · 用户消息（右对齐气泡）  │    │
│              │  │  · Agent 回复（左卡片）   │    │
│              │  │    · 执行过程展开区       │    │
│              │  │    · 最终 Markdown 报告   │    │
│              │  │    · 文件下载按钮         │    │
│              │  │                           │    │
│              │  ├─────────────────────────┤    │
│              │  │     底部输入框           │    │
│              │  └─────────────────────────┘    │
└──────────────┴─────────────────────────────────┘
```

#### 核心模块说明

**1. 左侧边栏**
- 品牌区：项目名「学术研究助手」+ 副标题
- 新建研究任务按钮（蓝色主按钮）
- 历史会话列表：支持切换历史对话，显示会话标题和时间
- 底部：三个智能体状态指示器（绿色圆点 = 在线）

**2. 顶部状态栏**
- 左侧：当前任务标题
- 右侧：执行状态指示（待命 / 执行中），带脉冲动画

**3. 对话流区域**
- **空状态**：欢迎页 + 三个示例问题快捷入口
- **用户消息**：右侧蓝色圆角气泡
- **Agent 回复卡片**：
  - 上半部分：执行过程时间线（带图标的事件列表，逐步显示）
  - 下半部分：最终 Markdown 格式研究报告
  - 底部：文件下载按钮（Markdown / PDF）

**4. 执行过程可视化**
- 每个事件带对应图标：🔀 调度助手 / ⚙️ 工具执行 / ✅ 工具完成 / 📄 结果生成
- 正在执行的事件显示加载动画
- 事件按时间顺序排列，用户能直观看到"主智能体 → 调度哪个助手 → 执行什么工具 → 返回什么结果"

**5. 底部输入区**
- 圆角输入框，支持 Enter 发送
- 右侧蓝色发送按钮
- 执行中按钮置灰，防止重复提交

#### 参考实现

前端 demo 已完成初版，位于 `frontend-demo/` 目录，可直接运行查看效果：

```bash
cd frontend-demo
npm install
npm run dev
```

访问 http://localhost:5173 即可看到白色背景的简洁版界面，包含左侧边栏、对话流、执行过程时间线、示例问题快捷入口等完整交互。后续在此基础上对接真实后端 WebSocket 即可。

#### 技术实现要点

**技术栈**：React + TypeScript + Vite + Tailwind CSS

**组件拆分**：
- `App.tsx` —— 主布局 + 状态管理
- `components/Sidebar.tsx` —— 左侧边栏
- `components/ChatMessage.tsx` —— 单条消息（用户/Agent）
- `components/EventTimeline.tsx` —— 执行过程时间线
- `components/ChatInput.tsx` —— 底部输入框
- `components/EmptyState.tsx` —— 空状态欢迎页

**与后端对接**：
- WebSocket 接收 monitor 推送的事件流
- 事件类型映射：`assistant_call` → 调度助手图标，`tool_start` → 工具执行图标，`task_result` → 完成图标
- 流式更新：每收到一个事件就追加到时间线，不用等全部完成

**样式设计**：
- 主色：#2563eb（蓝色）
- 背景：#ffffff（纯白）
- 边框：#e5e7eb（浅灰）
- 文字：#1f2937（深灰）/ #6b7280（中灰）/ #9ca3af（浅灰）
- 圆角：rounded-lg / rounded-xl

#### 简历亮点表述

> **前端界面重构**：基于 React + Tailwind CSS 重新设计前端界面，从原项目深色开发者工具风格改造为纯白背景的简洁现代风；设计多智能体执行过程可视化组件，通过事件时间线实时展示主智能体调度链路（助手调用 → 工具执行 → 结果返回），提升系统可观察性与用户体验



***

## 五、技术栈变更对照表



| 模块   | 原技术栈                 | 改造后                    | 变更说明    |
| ---- | -------------------- | ---------------------- | ------- |
| 检查点  | InMemorySaver        | SQLiteSaver            | 持久化会话记忆 |
| 子智能体 | 网络搜索 / 数据库 / RAGFlow | 网络搜索 / 数据分析 / 私有文档     | 职责重构    |
| 搜索工具 | Tavily 直接返回          | Tavily + Rerank + 语义缓存 | 检索优化    |
| 工具接入 | 硬编码 import           | MCP 协议（可选）             | 解耦工具层   |
| 前端   | 深色主题 + 事件流列表       | 纯白现代风 + 事件时间线可视化      | 界面重构 + 可视化 |



***

## 六、简历项目描述（最终版）

### 项目名称（你自己起）

**多智能体深度研究助手系统**（或叫 "学术研究智能助手"）

### 简历写法（200-300 字）

> **多智能体深度研究助手系统**
>
>  \| 个人项目 | 2026.09 - 至今
> 基于 LangGraph/DeepAgents 设计 Orchestrator-Workers 多智能体架构，主智能体负责任务规划与调度，三个专家子智能体分别处理外部信息检索、数据分析计算和私有文档问答，职责完全解耦。
> **多智能体架构**
>
> ：设计基于隔离沙箱的 Python 代码执行工具（subprocess 隔离 + 超时控制），实现数据统计与可视化能力；三个子智能体分别负责网络检索、数据分析和文档问答，避免职责重叠
> **会话持久化**
>
> ：将原有内存级 Checkpointer 替换为 SQLite 持久化存储，支持跨服务重启的会话恢复与历史记录管理，实现断点续传
> **检索优化**
>
> ：设计搜索结果重排序机制，基于 embedding 余弦相似度对检索结果做二次排序，降低主智能体上下文 token 消耗约 30%；实现语义缓存层，相似查询缓存命中率约 20%
> **前端可视化**
>
> ：基于 React + Tailwind CSS 重构前端界面，设计多智能体执行过程时间线组件，通过 WebSocket 实时展示助手调度与工具调用链路，提升系统可观察性



***

## 七、实施计划

### 阶段一：后端基础改造（第 1-3 天）

| 步骤 | 任务                                                | 产出       |
| -- | ------------------------------------------------- | -------- |
| 1  | 删除数据库相关代码（db\_tools.py、database\_query\_agent.py） | 代码清理完成   |
| 2  | 新增 Python 执行工具 python\_exec\_tool.py              | 沙箱代码跑通   |
| 3  | 新增 data\_analysis\_agent.py 子智能体                  | 多智能体架构跑通 |
| 4  | 修改 prompts.yml，更新主智能体和子智能体提示词                     | 三个助手职责清晰 |

**验收标准**：后端 API 跑通 "搜索 + 数据分析" 的完整链路。

***

### 阶段二：前端界面重构（第 4-5 天）

| 步骤 | 任务                                    | 产出                 |
| -- | ------------------------------------- | ------------------ |
| 1  | 搭建 React + Vite + Tailwind 前端项目骨架       | 前端项目可启动            |
| 2  | 实现整体布局：左侧边栏 + 右侧主区域                     | 页面结构完整             |
| 3  | 实现对话流组件：用户气泡 + Agent 卡片 + 执行时间线        | 对话界面可用             |
| 4  | 对接 WebSocket，实时展示执行事件流                   | 能看到智能体调用过程        |
| 5  | 实现空状态欢迎页 + 示例问题快捷入口                    | 首次打开体验好            |

**验收标准**：前端界面与后端联调完成，能完整展示一个研究任务从发起执行到生成报告的全过程。

***

### 阶段三：持久化改造（第 6-7 天）

| 步骤 | 任务                             | 产出          |
| -- | ------------------------------ | ----------- |
| 1  | 替换 InMemorySaver 为 SqliteSaver | 重启后会话不丢     |
| 2  | 新增历史会话列表接口                     | API 可查询历史会话 |
| 3  | 前端对接历史会话列表 UI                  | 能看到之前的对话    |

**验收标准**：服务重启后，之前的对话记录还在，能继续之前的任务。

***

### 阶段四：检索优化（第 8-10 天）

| 步骤 | 任务                                    | 产出                 |
| -- | ------------------------------------- | ------------------ |
| 1  | 集成 embedding 模型（本地 bge-small 或在线 API） | 可生成文本向量            |
| 2  | 实现搜索结果重排序                             | Tavily 返回后先 rerank |
| 3  | 实现语义缓存                                | 相似查询直接命中缓存         |
| 4  | 写个简单评测：对比改造前后的 token 消耗和准确率           | 有量化数据              |

**验收标准**：同样的查询，改造后 token 消耗明显下降，结果质量不降低。

***

### 阶段五：可选加分项（第 11-12 天）

| 步骤 | 任务               | 产出         |
| -- | ---------------- | ---------- |
| 1  | 封装 MCP Server    | 工具独立可运行    |
| 2  | 整理项目 README、部署文档 | 项目可交付      |



***

## 八、面试常见问题预判

### Q1：为什么要做三个子智能体？一个智能体加所有工具不行吗？

> A：一个智能体工具太多会导致模型选择困难，容易调错工具，而且所有工具的描述都占用上下文。拆成三个子智能体后，每个智能体只看到自己的工具，决策更精准，也更容易针对不同场景做优化。

### Q2：SQLite 做 Checkpointer 性能够吗？

> A：这个项目是单用户场景，SQLite 完全够。如果要上生产，可以换成 PostgreSQL。选 SQLite 的原因是零配置、易部署，个人项目阶段够用了。

### Q3：重排序为什么不用专门的 Rerank 模型？

> A：个人项目阶段用 embedding 余弦相似度做简单排序，成本最低、效果也够用。如果后续要提升精度，可以换成 cross-encoder 类的 Rerank 模型，架构上是可替换的。

### Q4：沙箱执行 Python 代码安全吗？

> A：现在的实现是进程级隔离，限制了执行时间和工作目录。生产环境应该用 Docker 容器做更强的隔离，或者用 e2b 这类专门的代码沙箱服务。



***

## 九、风险与注意事项



1. **不要直接照抄原项目结构和命名**：把目录名、变量名、注释风格改成你自己的，避免 GitHub 一搜就搜到一样的代码

2. **项目名字自己起**：别叫 "深度研搜"，叫 "学术研究助手" 之类的

3. **改完一定要自己跑通**：每个改造点都要实际验证过，面试才能讲清楚

4. **保留原项目的 git 历史**：方便后续对比改动，也能说明你确实是在这个基础上改的



***

*文档版本：v1.0*

*创建日期：2026-09-19*