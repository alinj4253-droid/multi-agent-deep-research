# 项目审查记录与待决策问题

> 审查时间：2026-09-20
> 审查范围：`Agent/` 根目录下全部文件（`deepsearch-agents/` 后端、`frontend-demo/` 前端、
> `searxng/` 搜索服务、根目录文档；`.venv/`、`node_modules/`、`.git/` 等依赖目录除外）
> 本次已**顺手修复**的问题见第一部分；**改动较大、需要你判断**的问题见第二部分。

---

## 一、本次已顺手修复的小问题（已验证、未提交）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `app/api/server.py` | **上传接口存在路径穿越风险**：`target_dir / file.filename` 直接使用客户端文件名，传入 `../x` 或带盘符路径可写到会话目录之外 | 改用 `Path(filename).name` 只保留文件名，非法名返回 400 |
| 2 | `app/api/server.py` | `/api/download`、`/api/files` 出错时返回 **HTTP 200 + `{"error":...}`**，浏览器直连下载会把错误 JSON 当文件下载 | 改为标准 400/403/404/500 状态码（`HTTPException`），前端提示逻辑不受影响 |
| 3 | `app/api/server.py` | `TaskRequest.thread_id: str = None` 类型标注错误（应为 Optional） | 改为 `Optional[str] = None` |
| 4 | `app/agent/main_agent.py` | 异常提示错别字「执行主智能发生异常信息」 | 改为「执行主智能体时发生异常」 |
| 5 | `app/tools/python_exec_tool.py` | 子进程超时后只 `kill()` 未 `wait()`/回收管道，且不清理 `_temp_exec.py`，可能残留僵尸进程与临时文件 | 超时后 `await communicate()` 回收并删除临时文件 |
| 6 | `app/tools/python_exec_tool.py` | `_session_call_counts` 字典长期运行只增不减（内存泄漏） | 增加 >200 条时淘汰最早 100 条的防护（与检索预算一致） |
| 7 | `app/tools/academic_sources.py` | OpenAlex 解析里 `venue = ""` 重复赋值（死代码） | 合并为一次表达式 |
| 8 | `app/tools/searxng_search.py` | `_search_http` 的 `encoded` 形参未使用；**SearXNG 不支持条数参数，`max_results` 实际不生效**（要 3 条会返回 30+ 条，浪费上下文） | 删除多余形参；两个通道返回前统一按 `max_results` 截断（实测 3 条→3 条） |
| 9 | `app/prompt/prompts.yml`、`app/agent/subagents/network_search_agent.py` | 网络助手在 YAML 中仍沿用历史键名 `tavily`，模块 docstring 写「使用 DuckDuckGo」 | YAML 键改名为 `network_search`，代码同步更新；docstring 改为「SearXNG 为主、DuckDuckGo 兜底」 |
| 10 | `app/tools/ddg_search.py` | docstring 仍写「作为 Tavily 的降级」 | 改为「作为自建 SearXNG 的降级」 |
| 11 | `.vscode/launch.json` | 调试入口指向**已不存在**的模块 `app.agent.graph`，F5 必报错 | 改为 `uvicorn app.api.server:app --port 8001` |
| 12 | `frontend-demo/src/App.tsx` | 事件样式里有后端从不发送的 `tool_end` 死分支 | 删除 |
| 13 | `deepsearch-agents/.env` | 残留 Tavily/MySQL 旧变量，缺少 SearXNG 四项配置（靠代码默认值兜底） | 重写为当前结构：DeepSeek / OpenAlex / SearXNG / RAGFlow 分组，Tavily key 降级为注释保留（未删除原值） |

**修复后验证**：`pytest -q` **51 passed**；前端 `tsc -b` 零错误；SearXNG 桥接检索、
学术三源直连均实测通过；后端已用修复后的代码重启。

---

## 二、改动较大 / 需要你决策的问题（本次只记录，未动）

### P0 — 影响功能完整性

1. **前端没有文件上传入口，但示例按钮引导用户「分析上传的数据集」**
   - 现状：后端 `/api/upload`、前端 hook 的 `uploadFiles`、上传状态/类型都已写好，
     但 `App.tsx` **没有任何上传 UI**（未解构 `uploadFiles/uploadedItems`）。
     空状态第二个示例「📊 分析上传的数据集，做性能对比」点击后无文件可分析，
     只能让模型自行造数，体验与文案不符。
   - 建议（二选一）：
     - (a) 在输入区加一个附件按钮 + 已选文件列表（工作量小，后端已就绪，推荐）；
     - (b) 先把该示例按钮改成不依赖上传的文案。

2. **历史会话列表是写死的**
   - 现状：SQLite（`app/checkpoints.db`，已 17MB）里持久化了全部会话，但侧边栏固定显示
     「暂无历史会话」；后端也没有列出/恢复 thread 的接口。
   - 建议：新增 `GET /api/threads`（从 checkpointer 表聚合最近会话首条消息与时间），
     前端侧边栏渲染并支持点击恢复（thread_id 已存 localStorage，恢复机制本身可用）。

3. **`ragflow-sdk` 声明了依赖但环境里没装，两个 RAGFlow 模块一 import 就报错**
   - `app/tools/ragflow_tools.py`、`app/ragflow/knowledge_demo.py` 顶部
     `from ragflow_sdk import RAGFlow`，实测 `ModuleNotFoundError: No module named 'ragflow_sdk'`。
   - 主链路不导入它们，所以不影响运行；但 `requirements.txt` / `pyproject.toml` 都列了
     `ragflow-sdk>=0.1.0`，与实际环境不一致。
   - 建议（三选一）：① 确认 RAGFlow 不再需要，删除这两个文件 + 依赖声明（最干净）；
     ② 改为延迟导入（try/except），让模块可被 import、调用时再给友好提示；
     ③ 真正 `pip install ragflow-sdk` 并接线。

### P1 — 健壮性 / 安全

4. **`app/utils/path_utils.py` 的 `updated/` 分支按进程 CWD 解析，路径基准不对**
   - 代码：`return str(Path(relative_part).resolve())`，相对的是启动目录
     （`deepsearch-agents/`），而上传文件实际落在 `deepsearch-agents/app/updated/`。
     若模型传入 `updated/session_xxx/f.csv` 会解析到不存在的位置。
   - 当前主流程会把上传文件复制进会话目录并要求模型用**纯文件名**读取，所以该分支
     实际几乎不触发，属潜伏问题。建议改为相对 `app/` 目录（`project_root`）解析。

5. **CORS 配置为 `allow_origins=["*"]` + `allow_credentials=True`**
   - 本地教学项目无妨，但二者组合在浏览器规范下并不生效（带凭证时不允许通配源）。
     建议把源收敛为 `http://localhost:5173`（或用环境变量配置）。

6. **SearXNG `secret_key` 明文提交在 `searxng/settings.yml`**
   - 本地单用户实例风险很低；若日后分享/公开仓库，建议改为读环境变量或先换一个 key。

7. **上传/文件列表接口在事件循环里做同步磁盘 IO**
   - `shutil.copyfileobj`、`rglob` 遍历都是阻塞调用，大文件或产物很多时会卡住事件循环。
     建议用 `aiofiles` / `run_in_threadpool`（依赖里已有 aiofiles）。

### P2 — 清理 / 一致性 / 文档

8. **遗留前端 `deepsearch-agents/frontend/`（Ant Design 版，默认连 8000 端口、pnpm）**
   与在用的 `frontend-demo/`（5173 端口、npm）并存，容易误启动。建议在其 README 顶部
   标注「上游原始前端，已弃用」或整体移出运行目录。

9. **遗留 MySQL：`docker/docker-compose.yaml` + `docker/mysql/mysql.sql`** 已不被使用
   （现用 SQLite）；compose 注释写 3307、默认值却是 3306。建议归档到 `legacy/` 或删除。

10. **临时/日志文件散落**：`deepsearch-agents/_tmp_diag3.log`、`_tmp_diag_data.log`
    是一次性诊断产物（已被 `.gitignore` 的 `*.log` 覆盖，不进 Git）；`app/output/session_*`
    历史会话产物持续累积。建议清理 diag 日志，并考虑给 output 加定期清理策略。

11. **`uv.lock` 已过期**：仍锁着 `tavily-python==0.7.24`，但 `pyproject.toml` 已移除 Tavily。
    若继续用 uv 管理，需 `uv lock` 重新生成；当前用 pip + venv 运行则无影响。

12. **pre-commit 配置引用了未声明的 ruff/uv**：`.pre-commit-config.yaml` 用
    `uv run ruff`，但 dev 依赖里只有 pytest，没有 ruff。启用 pre-commit 前需补依赖。

13. **文档端口不一致**：《项目进度总览与收尾计划.md》第六节写前端 `http://localhost:5174`，
    实际 Vite 默认且实测为 **5173**（根 README 正确）。建议顺手改正。

14. **arXiv 偶发超时无重试**：三源容错已能用 OpenAlex/Crossref 兜底，不影响结果；
    如追求更稳，可给 arXiv 单源加 1 次重试或把超时从 15s 略增（文档「观察项」已记录）。

15. **上游教程残留**：`examples/`（含 `from tavily import TavilyClient`）、
    `deepsearch-agents/README.md` 的 Tavily/MySQL/8000 端口章节均为上游原始教程，
    README 顶部已声明「以本地运行指南为准」，保留无害，知道即可。

---

## 三、本次启动与验证结果（2026-09-20）

| 组件 | 状态 | 地址 / 说明 |
|------|------|------|
| SearXNG | ✅ 运行中 | WSL2 `Ubuntu-20.04` Docker 容器 `searxng`，容器内 8888；Windows 直连被代理 TUN 劫持，后端自动走 **wsl 桥接通道**（实测 32 条结果、热查询约 2.5s） |
| WSL 保活 | ✅ 已启动 | 已用**隐藏常驻 wsl 进程**方式启动 `keep_wsl_alive.sh`（见下方「启动中排查并解决的环境问题」） |
| 后端 | ✅ 运行中 | http://localhost:8001 ，接口文档 http://localhost:8001/docs |
| 前端 | ✅ 运行中 | **http://localhost:5173** |
| 单元测试 | ✅ 51 passed | 不依赖外网/大模型 |
| 常识直答 E2E | ✅ 1.4s | 零工具调用，直接作答 |
| 网络检索 E2E | ✅ 39s | 正确路由网络助手，3 次预算内 SearXNG 检索，报告带来源链接、主动标注信息缺口 |
| 数据分析 E2E | ✅ 通过 | 沙箱出统计表 + PNG 直方图，产物清单正确回传 |
| 学术三源直连 | ✅ 全可用 | arXiv / OpenAlex / Crossref 三源均返回，无失败源 |
| Markdown/PDF | ✅ 历史已验证 | `backend.run.log` 留有成功生成 .md + .pdf 并下载 200 的记录 |

### 启动中排查并解决的环境问题：Docker/SearXNG 反复冷启动

- 现象：启动后发现 `searxng` 容器每隔 20–40 秒就被**重新创建**一次（`RestartCount=0`、
  `ExitCode=0`，但 `StartedAt` 不断刷新），每次冷启动约 10–15 秒内检索不可用。
- 定位：WSL（已运行 4 小时，未关停）里的 **dockerd 在没有前台 WSL 会话时被回收**，
  下次 `docker` 命令经 socket 激活重新拉起 dockerd，容器随之重启。
  对照实验证实：保持一个 WSL 会话常开时，容器 uptime 连续增长、不再重启。
- 尝试 `wsl ... "nohup keep_wsl_alive.sh &"` 的方式**无效**——wsl.exe 退出后，
  会话内后台进程仍会被回收。
- 最终采用 README 记载的方式，在 **Windows 侧启动隐藏的常驻 wsl 进程**（已执行）：
  ```powershell
  Start-Process wsl -WindowStyle Hidden -ArgumentList '-d','Ubuntu-20.04','--','bash','<项目根目录>/searxng/keep_wsl_alive.sh'
  ```
  启动后 dockerd 稳定、容器 uptime 持续增长、热查询约 2.5s。
- **注意**：该常驻进程在电脑重启/注销后会消失，需要重新执行上面的命令；
  如需开机自启，可注册到 Windows「任务计划程序（登录时触发）」。

### 人工测试建议路径

1. 打开 http://localhost:5173 ，左下角应为绿色「已连接」。
2. 常识直答：「用一句话解释什么是光合作用」——应不派任何助手、秒回。
3. 网络检索：「检索 Python 3.13 的主要新特性，带来源链接」——应看到「网络检索助手」
   派单 + 3 次「网络搜索工具」事件 + 带来源表格的报告。
4. 学术文献：「检索 4D Gaussian Splatting 的代表性论文，按年份和引用梳理脉络」
   ——应派「学术文献助手」，报告含真实引用数/DOI。
5. 数据分析：「用 Python 生成正态分布随机数并画直方图保存为 PNG」——应派
   「数据分析助手」，结束后报告下方出现可下载的 PNG。
6. 新建任务、取消任务、刷新页面（同一会话恢复）各试一次。

> 注：每次全新对话前端会生成新 thread_id；检索预算按 thread 计数（网页 3 次、学术 2 次）。
