# 多智能体深度研究助手・项目进度总览与收尾计划

> 仓库根目录：
>
> `<项目根目录>`
> 后端：
>
> `deepsearch-agents/`
>
> （FastAPI + LangGraph DeepAgents + SQLite 持久化）
> 前端：
>
> `frontend-demo/`
>
> （React + TS + Vite + Tailwind，白底工作台）
> 更新时间：2026-09-20



***

## 一、项目是什么

基于开源 `didilili/deepsearch-agents` 二次开发的**多智能体深度研究助手**：用户用自然语言提出研究问题，系统由一个 "主智能体" 理解任务、分派给专职 "子智能体" 检索 / 计算，再汇总成带引用的结构化研究报告，前端通过 WebSocket 实时展示 "思考 → 派单 → 调工具 → 成文" 全过程。

### 架构确认：是中心化（Hub-and-Spoke / Orchestrator-Worker）

你问的 "是不是中心化"——**是的**。当前是典型的**中心化编排架构**：



```
&#x20;                         ┌────────────────────────────────────┐

&#x20;                         │   主智能体（Orchestrator，中心化节点） │

&#x20;                         │   理解任务 · 意图路由 · 单次委派约束    │

&#x20;                         │   汇总子智能体结果 · 撰写最终报告      │

&#x20;                         └──────────────────┬─────────────────┘

&#x20;             ┌──────────────────┬──────────┴─────────┬──────────────────┐

&#x20;             ▼                  ▼                    ▼                  ▼

&#x20;     网络检索助手          数据分析助手          学术文献助手        文件工具

&#x20;     (Worker)             (Worker)             (Worker)        （主智能体直持）

&#x20;     SearXNG 主           execute\_python       arXiv

&#x20;     \+ DuckDuckGo 备      代码计算/图表         OpenAlex

&#x20;     熔断/故障转移                              Crossref（双路）
```



* 子智能体之间**不互相调用、不横向通信**，全部由主智能体统一派单和收口；

* 主智能体对每个子智能体 "一次任务只委派一次"，避免反复派单和死循环；

* 这是 deep research 类系统（GPT Researcher、LangGraph supervisor 等）的主流、可解释、可控的架构，面试时讲得清 "谁在什么条件下调谁、怎么收口"。



***

## 二、模型配置（本次已重构为 DeepSeek）

### 2.1 结论



* 供应商：**DeepSeek 官方 OpenAI 兼容端点**

* Base URL：`https://api.deepseek.com/v1`

* 主智能体、三个子智能体**统一使用&#x20;**`deepseek-flash`（即你说的 V4.1-Flash 在 API 里的真实模型名）。

* 该账户 `/v1/models` 实际只有两个模型：`deepseek-flash`（快）、`deepseek-v4-pro`（更强，备用）。


  * 注意：`DeepSeek-V4.1-Flash` / `deepseek-v4.1-flash` 作为模型名会报 400，正确名字是 `deepseek-flash`；`deepseek-chat`、`deepseek-reasoner` 是旧别名，不建议显式使用。

* API Key 已写入 `deepsearch-agents/.env`（该文件被 `.gitignore` 忽略，**不会进 Git、不硬编码进源码**）。

### 2.2 已实测（事实，非推测）



| 检查项                                              | 结果                                                              |
| ------------------------------------------------ | --------------------------------------------------------------- |
| Key + Base URL 连通                                | `GET /v1/models` 返回 200，列出 `deepseek-flash` / `deepseek-v4-pro` |
| `deepseek-flash` 普通对话                            | 200，正常                                                          |
| `deepseek-flash` function-calling（工具调用，多智能体关键能力） | **支持**，正确产出 `tool_calls`，单次约 **1.2s**                           |
| 学术端到端（DeepSeek flash）                            | **35 秒**跑通，路由准确、单次检索、报告真实可溯源                                    |

> 对比：此前主智能体用阿里云百炼 qwen3.8-27b 时学术端到端 152–270 秒；换 DeepSeek flash 后 
>
> **35 秒**
>
> ，提速约 4–8 倍，且工具调用稳定、报告事实纪律好（会主动标注 "某源超时 / 数据缺口"，不编造）。
>
> **延迟问题基本解决。**

### 2.3 改了哪些文件



* `deepsearch-agents/.env`：`OPENAI_BASE_URL`、`OPENAI_API_KEY` 改为 DeepSeek；模型变量统一为

  `LLM_MAIN_MODEL=deepseek-flash`、`LLM_FAST_MODEL=deepseek-flash`。

* `deepsearch-agents/app/agent/llm.py`：重写为通用初始化，显式传入 `api_key/base_url`，

  暴露 `model`（主）与 `fast_model`（子）两个对象；想让主智能体换更强模型，只改 `.env` 的

  `LLM_MAIN_MODEL=deepseek-v4-pro` 即可，无需改代码。

* 运行链路 `app/` 内已无任何 qwen / 旧变量名残留（`examples/` 是原项目自带教程，不在运行链路，保留未动）。

### 2.4 费用与免费边界（按你的要求）



* 大模型：走你**已充值的 DeepSeek 账户**（你已授权统一使用）。

* 检索 / 搜索全部免费、不绑卡：本地自建 SearXNG、DuckDuckGo、arXiv、OpenAlex（已配免费 key）、Crossref。

* 未开通任何新付费服务；后续凡涉及额外付费，都会先征得你同意。



***

## 三、已完成的工作（累计进度）

### 阶段 1–3（已 Git 提交，12 个 commit）



1. 克隆并吃透原项目，产出《改造方案.md》；

2. 后端由原 "网络搜索 (Tavily)+MySQL+RAGFlow 一主三从" 重构为三助手；

3. 会话持久化 InMemorySaver → **AsyncSqliteSaver**（SQLite）；

4. 前端白底工作台重构、对接真实后端、**WebSocket 实时事件流**、Markdown 报告渲染；

5. 移除付费 Tavily，改免费检索 + 检索次数硬预算。

### 阶段 4（代码已完成并通过测试，尚未提交）



1. **本地自建 SearXNG 元搜索**（WSL2 + 原生 Docker Engine，免 Docker Desktop）：

* 容器 `searxng`，聚合 Google/Bing/DuckDuckGo/Brave 等，自带 JSON API，`restart:always`；

* 解决两个硬问题：配置文件直挂 `/etc/searxng` 触发 granian 重启循环（改挂 `/config` + entrypoint 拷贝）；

  代理 TUN 劫持 Windows→WSL 入站（否决 mirrored / 直连 IP/netsh 后，用免提权 `wsl` 命令通道 + `wsl_query.sh` 15 次轮询重试）；

* `.wslconfig` 设 `vmIdleTimeout=-1` 让容器常驻；实测热查询 1.3–2s。

1. **网络检索 provider 抽象 + 熔断 + 故障转移**：SearXNG 主 → DuckDuckGo 备，

   `CircuitBreaker`（连续失败开路、冷却半开试探）、按会话检索预算、LRU 缓存、关键词重排，成功结果带 `provider` 字段。

2. **空壳 "私有文档助手" → 真实 "学术文献助手"**：

* 三源并发：arXiv（Atom XML）、OpenAlex（倒排摘要重建、引用数、OA 链接）、Crossref（DOI、JATS 清洗）；

* **Crossref 双路检索**：相关性路（偏新）+ 按被引降序路（召回奠基经典），高被引路用 "标题判别词重叠" 过滤跨领域噪声（曾混入医学高被引论文）；

* 跨源标准化标题去重、来源标签合并去重、按（被引→年份→标题完整度）排序；摘要截断 500 字、作者 ≤6；

* OpenAlex 配免费 key 解决共享出口 IP 的 429 限流；429 给可读降级提示。

1. **模型分层 / 统一**：主、子模型可独立配置（现统一 DeepSeek flash）。

2. **提示词工程**：三助手路由边界、单次委派纪律、学术检索词 "2–5 个核心术语、不堆砌"、

   报告 "只用真实检索材料、不凭记忆补全、默认 600–1000 字、一个主表 + 3–5 要点"。

3. **前端同步**：第三助手改为 "学术文献助手"、空状态示例改为学术检索。

4. **测试**：新增熔断器、网络故障转移、学术三源解析 / 双路过滤 / 来源去重等离线单测，

   **当前&#x20;**`pytest -q`**&#x20;共 51 个全部通过**（不依赖外网 / 大模型）。

### 本次 DeepSeek 冒烟（已通过）



1. 全新 thread 学术问题：正确路由学术助手 → 单次三源检索（本次 arXiv 超时，OpenAlex+Crossref 兜底）

   → 35 秒产出《4D Gaussian Splatting 代表性论文脉络》，3DGS (6249)/4DGS (816)/2DGS (790)/Mip-Splatting (606)

   等均真实、带 DOI/venue/ 引用，并主动标注数据缺口。



***

## 四、项目当前位置（完成度判断）



* **核心架构与功能：约 90% 已完成**。中心化一主三从、双路异构检索（网页 + 学术）、SQLite 持久化、

  WebSocket 事件流、白底前端、模型统一 DeepSeek、51 个单测 —— 这些主干都已落地。

* **学术链路已在 DeepSeek 上端到端跑通且很快（35s）**。

* 现在处于 \*\*"收尾验证 + 清理 + 文档 + 提交" 阶段 \*\*，不再有大的架构改动。

一句话：**已经能跑出真实结果，剩下的是把另外几条链路也在 DeepSeek 下回归一遍、在浏览器里完整演示一遍、清理临时文件、补齐文档和 Git 提交，即可达到 "可写简历、可现场演示、可复现部署" 的交付状态。**



***

## 五、距离 "真正跑通整个项目" 还差的步骤

### P0｜DeepSeek 下全链路端到端回归（核心，约 4 个用例）



| # | 用例                   | 预期                                      | 状态               |
| - | -------------------- | --------------------------------------- | ---------------- |
| 1 | 学术论文检索               | 路由学术助手、三源 / 双路召回、真实报告                   | ✅ 已通过（35s）       |
| 2 | 网络检索（新闻 / 开源动态 / 教程） | 路由网络助手、工具走 SearXNG（事件可见 provider）、带来源链接 | ⏳ 待回归（之前在百炼下被中断） |
| 3 | 数据分析（计算 / 画图）        | 路由数据助手、`execute_python_code` 正确、图表落盘    | ⏳ 待回归            |
| 4 | 常识直答                 | 主智能体**零工具调用**直接回答                       | ⏳ 待回归            |

> 每个用例都用
>
> **全新 thread**
>
> （旧 thread 会复用 SQLite 记忆、不会重新检索）。

### P1｜前端浏览器联调（"真正跑通" 的最后一公里）



1. 启动前端 `npm run dev`，在浏览器 `http://localhost:5173` 完整走一遍：

   提问 → 看团队 / 派单高亮 → 工具事件（学术检索 / 网络搜索 SearXNG / 代码执行）→ 最终 Markdown 报告渲染、表格 / 链接正常；

   确认无控制台报错、无之前那种 "工具名刷屏"。

### P2｜清理、文档、提交



1. **清理临时脚本**（当前目录下约 12–15 个 `diag_*.py / test_*.py / probe_*.py / time_*.py / smoke_*.py`，

   以及 `searxng/` 下一次性诊断 `.sh`）；`e2e_run.py` 可移到 `scripts/` 作为手工回归脚本保留。

2. **更新文档**：`README.md`、《改造方案.md》同步 —— 中心化一主三从、DeepSeek 模型配置、

   SearXNG 自建与双 transport、学术三源 + Crossref 双路、熔断故障转移、WSL/Docker 部署与 `.wslconfig`、

   免费声明、运行手册、简历 bullet。

3. **分阶段 Git 提交**（第四阶段 + 模型重构目前全部未提交），建议分组：

   SearXNG 部署 / 检索治理与网络故障转移 / 学术三源与学术助手 / 模型统一 DeepSeek /

   前端同步 / 测试 / 文档；每次提交前 `pytest -q` 全绿、确认无临时文件与密钥。

### 观察项（不一定阻塞，记录在案）



* **arXiv 偶发超时**：本次冒烟 arXiv 一次超时（15s），三源容错已用 OpenAlex+Crossref 兜底、质量未受影响；

  可后续给单源加 1 次重试或略增超时，非阻塞。

* **报告导出 Markdown/PDF**：代码存在但 `output/` 从未实测产出，建议作为单独用例验证或明确标注未启用。

* **历史会话列表 UI**：SQLite 已存数据，前端侧边栏仍写死 "暂无历史会话"，属遗留增强项。

* DeepSeek 如需更高成文质量，可只把主智能体切到 `deepseek-v4-pro`（改一个环境变量），子智能体保持 flash。



***

## 六、如何运行（恢复后操作手册）



```
\# 1) SearXNG（一般已常驻；开机后若没起）

wsl -d Ubuntu-20.04 -- bash -lc "cd <项目根目录>/searxng && docker compose up -d"

\# 2) 后端（端口 8001，读取 .env 里的 DeepSeek 配置）

cd <项目根目录>\deepsearch-agents

.\\.venv\Scripts\python.exe -m uvicorn app.api.server:app --host 0.0.0.0 --port 8001

\# 3) 前端（浏览器开 http://localhost:5173）

cd <项目根目录>\frontend-demo

npm run dev

\# 4) 跑测试

cd <项目根目录>\deepsearch-agents

.\\.venv\Scripts\python.exe -m pytest -q
```



***

## 七、简历亮点 Bullet（更新版，可直接改用）



* 基于 LangGraph DeepAgents 设计实现**中心化编排的一主三从多智能体深度研究系统**：主智能体统一负责意图路由、单次委派约束与报告收口，网络检索 / 学术文献 / 数据分析三个子智能体专职执行、互不横向耦合，全过程经 FastAPI + WebSocket 推送可观测事件流，AsyncSqliteSaver 持久化会话。

* **本地自建 SearXNG 元搜索**（WSL2 + Docker，聚合 Google/Bing/DuckDuckGo 等 70+ 源），针对代理 TUN 劫持入站流量设计 HTTP/WSL 双通道自动故障转移与轮询重试，热查询 1–2s。

* 构建**多源异构检索层**：网络侧 SearXNG 主 + DuckDuckGo 备并配熔断器 / 检索预算 / LRU 缓存；学术侧并发聚合 arXiv/OpenAlex/Crossref，Crossref 采用 "相关性 + 按被引排序" 双路召回并用标题判别词过滤跨领域噪声，跨源去重按被引排序，单源失败 / 限流自动降级。

* 统一接入 **DeepSeek（OpenAI 兼容）function-calling 模型**，主 / 子模型可配置分层；通过提示词约束单次委派、检索词精简与 "仅基于真实检索材料成文"，将学术类深度研究端到端延迟从 4 分钟级压缩到 **约 35 秒**，报告含真实引用 / DOI 且可溯源。

* 为熔断器、故障转移、学术三源解析与双路过滤编写 **51 个离线单元测试**全部通过；前端 React + TS + Vite + Tailwind 实时渲染研究过程与 Markdown 报告。



***

## 八、恢复后建议执行顺序



1. 先跑 P0 剩余 3 个用例（网络 / 数据 / 常识），有问题就地修；

2. 浏览器 P1 联调，确认演示闭环；

3. P2 清理临时脚本 → 更新 README / 改造方案 → 分阶段 Git 提交；

4. 提交后打 tag / 截图，作为简历作品的可复现交付物。